# The frontend's API routes (app/api/**) import src/lib/supabase.ts directly and call VAPI directly,
# so they need the same credentials as the backend. Copy the values from the root .env.
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
VAPI_API_KEY=
